export const HOURS_SORT_FIELDS = {
  WORK_DATE: "workDate",
  CREATED_AT: "createdAt",
  HOURS: "hours"
};

export const HOURS_SORT_FIELD_VALUES = Object.values(HOURS_SORT_FIELDS);

export const HOURS_DEFAULT_PAGE = 1;
export const HOURS_DEFAULT_LIMIT = 50;
export const HOURS_MAX_LIMIT = 100;
